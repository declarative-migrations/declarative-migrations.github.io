{
  "name": "declarative-migrations.github.io",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "astro": "^5.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
